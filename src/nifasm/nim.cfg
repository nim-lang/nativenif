# nifasm now reads NIF through the new nifcore libraries (like arkham), so it
# needs nimony's lib on the path, plus the shared `src/common` dir for the
# foreign-module loader (`nifmodules`) used by both backends.
--path: "../../../nimony/src/lib"
--path: "../common"
--warning[UnusedImport]: off
