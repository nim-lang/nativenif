(.nif24)
(stmts
  (proc :main.0
    (params)
    (result)
    (clobber)
    (stmts
      (var :missing.0 (s) Missing.0.no_such_mod)

      (mov (rax) +60)
      (mov (rdi) +0)
      (syscall)
    )
  )
)
