(.nif27)
(.indexat 533              )
(stmts
 (proc :main.0
  (params)
  (result)
  (clobber)
  (stmts <unknown token> Valid <unknown token> cfvar declared and used exactly once
   (cfvar :tmp.0)
   (mov
    (rax)\2B0)
   (cmp
    (rax)\2B0)
   (ite
    (zf)
    (stmts
     (jtrue tmp.0))
    (stmts))
   (mov
    (rax)\2B99)<unknown token> cfvar used exactly once here
   (ite tmp.0
    (stmts
     (mov
      (rax)\2B42))
    (stmts
     (mov
      (rax)\2B1)))
   (mov
    (rdi)
    (rax))
   (mov
    (rax)\2B60)
   (syscall))))(.index)