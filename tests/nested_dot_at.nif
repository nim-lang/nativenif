(.nif24)
(stmts
  (type :Point.0 (object
    (fld :x.0 (i +64))
    (fld :y.0 (i +64))
  ))

  (proc :main.0
    (params)
    (result)
    (clobber)
    (stmts
      (var :points.0 (s) (array Point.0 +2))
      (var :arrptr.0 (rdi) (aptr Point.0))

      (mov (rbx) +7)
      (mov (mem (dot (at points.0 +1) x.0)) (rbx))
      (mov (rbx) +9)
      (mov (mem (dot (at points.0 +1) y.0)) (rbx))

      (lea (rdi) points.0)
      (mov (r8) (mem (dot (at arrptr.0 +1) x.0)))
      (mov (r9) (mem (dot (at arrptr.0 +1) y.0)))
      (kill arrptr.0)

      (cmp (r8) +7)
      (jne fail.0)
      (cmp (r9) +9)
      (jne fail.0)

      (mov (rdi) +0)
      (jmp exit.0)

      (lab :fail.0)
      (mov (rdi) +1)

      (lab :exit.0)
      (mov (rax) +60)
      (syscall)
    )
  )
)
