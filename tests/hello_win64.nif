(stmts
  (arch win_x64)
  (imp "kernel32.dll")
  (extproc :GetStdHandle.0 "GetStdHandle")
  (extproc :WriteConsoleA.0 "WriteConsoleA")
  (extproc :ExitProcess.0 "ExitProcess")

  (sub (rsp) +40)
  (mov (rcx) -11)
  (iat GetStdHandle.0)
  (mov (rdi) (rax))
  (add (rsp) +40)

  (sub (rsp) +48)
  (mov (rcx) (rdi))
  (lea (rdx) msg.0)
  (mov (r8) +12)
  (mov (r9) +0)
  (iat WriteConsoleA.0)
  (add (rsp) +48)

  (sub (rsp) +40)
  (mov (rcx) +0)
  (iat ExitProcess.0)

  (rodata :msg.0 "Hello World\0A")
)
