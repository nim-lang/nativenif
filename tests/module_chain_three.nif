(.nif24)
(stmts
  (proc :main.0
    (params)
    (result)
    (clobber)
    (stmts
      (var :level.0 (s) Level1.0.mod_level1)

      (mov (rax) +60)
      (mov (rdi) +0)
      (syscall)
    )
  )
)
